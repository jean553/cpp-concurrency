/**
 * @brief example of simple thread usage
 */

/**
 * @brief displays the numbers from the starting number to the ending number
 *
 * @param start the starting number
 * @param end the ending number
 */
void displayFromTo(
    const unsigned short& start,
    const unsigned short& end
);

/**
 * @brief starts a simple thread function
 */
void runSimpleThread();
